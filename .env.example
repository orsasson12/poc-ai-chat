# ============================================
# BizAssist AI — Environment Variables
# ============================================
# Copy this file to .env.local and fill in your keys.
# The app works with mock data when keys are missing.
# ============================================

# --- Supabase (Auth + Database + Storage) ---
NEXT_PUBLIC_SUPABASE_URL=https://supabase.com/dashboard/project/zsighwxnmtbceytgsvym
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# --- OpenAI (Embeddings + Chat + Moderation) ---
OPENAI_API_KEY=

# --- Anthropic (Failover LLM) ---
ANTHROPIC_API_KEY=

# --- Pinecone (Vector Store) ---
PINECONE_API_KEY=
PINECONE_INDEX=bizassist-prod

# --- Stripe (Billing) ---
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=

# --- Inngest (Job Queue) ---
INNGEST_EVENT_KEY=
INNGEST_SIGNING_KEY=

# --- Upstash Redis (Rate Limiting) ---
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# --- Cloudflare R2 (Widget CDN) ---
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_R2_ACCESS_KEY=
CLOUDFLARE_R2_SECRET_KEY=
CLOUDFLARE_R2_BUCKET=bizassist-widget

# --- Monitoring ---
# Sentry — error tracking (client + server)
SENTRY_DSN=
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_ENVIRONMENT=development
SENTRY_TRACES_SAMPLE_RATE=1.0
# Axiom — structured logs / business events (server-side only)
AXIOM_TOKEN=
AXIOM_DATASET=bizassist
AXIOM_ORG_ID=

# --- Retrieval Quality ---
# Query rewriting via Claude Haiku (off by default; turn on per-env)
RAG_QUERY_REWRITE_ENABLED=false
# Half-life for freshness decay boost (days). Older knowledge items lose ground to fresher ones but never drop below the floor.
RAG_FRESHNESS_HALF_LIFE_DAYS=180
# Minimum retained multiplier — evergreen content keeps at least this share of its semantic score.
RAG_FRESHNESS_FLOOR=0.6

# --- App Config ---
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_WIDGET_CDN_URL=http://localhost:3000
CANARY_SALT=change-me-to-a-random-string
